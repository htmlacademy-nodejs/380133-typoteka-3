extends ../wrappers/wrapper-color.pug

block content
  main
    section.search-page.search-page__wrapper
      h1 Поиск
      .search.search-page__form
        form(action='/search' method='get' autocomplete="off")
          label
            input(type='text' name='search' placeholder='Что ищем?' value=search)
          button.search__button.button.button--transparent(type='submit') Найти
        if search
          if results && results.length
            each result in results
              .search__result
                time.search__date(datetime=result.createdDate) #{result.createdDate}
                a.search__link #{result.title}
          else
            p.search-page__empty Ничего не нашлось

